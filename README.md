# Laravel to Vanilla PHP Test System

Hybrid project using:
- Vanilla PHP (legacy)
- Laravel (modern backend)

## Local Setup

1. Clone repository
2. Install dependencies

```bash
cd laravel
composer install
cp .env.example .env
php artisan key:generate
